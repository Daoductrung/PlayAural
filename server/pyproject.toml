[project]
name = "playaural-server"
version = "1.0.4.14"
description = "PlayAural Game Server"
requires-python = ">=3.11"
dependencies = [
    "websockets>=12.0",
    "mashumaro>=3.11",
    "fluent-runtime>=0.4.0",
    "babel>=2.14",
    "openskill>=6.1.3",
    "argon2-cffi>=23.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-xdist>=3.5",
]

[project.scripts]
playaural-server = "server.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
dev-mode-dirs = [".."]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
pythonpath = [".."]

[tool.hatch.build.targets.wheel]
only-include = [
    "__init__.py",
    "__main__.py",
    "audio.py",
    "cli.py",
    "main.py",
    "menu_pagination.py",
    "administration",
    "auth",
    "core",
    "documentation",
    "games",
    "game_utils",
    "locales",
    "messages",
    "network",
    "persistence",
    "tables",
    "ui",
    "users",
    "voice",
]

[tool.hatch.build.targets.wheel.sources]
"" = "server"
